/**
 * Application Configuration — HonSoc Website
 * 
 * Shared constants, thresholds, and institutional parameters.
 */

/**
 * Dean's List GWA Eligibility Cutoff Threshold.
 * Must remain null until official criteria is confirmed by the College of Arts and Sciences.
 */
export const DL_GWA_THRESHOLD: number | null = null; // PENDING CLIENT CONFIRMATION
